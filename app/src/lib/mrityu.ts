/**
 * МРИТЬЮ БХАГА — «градусы смерти»: по одному критическому градусу на каждую
 * граху в каждом знаке. Граха, попавшая в свой градус, считается лишённой сил:
 * классика читает это не как физическую смерть, а как жёсткие кармические
 * уроки в её периоды (даши). Просьба Георгия от 03.08.2026, п.7.
 *
 * ⚠ ОТКУДА ЧИСЛА. Таблица — 12×11 чисел, ошибиться в одном значит молча
 * соврать астрологу. Взята НЕ из ответа ИИ (проверено: разные прогоны Google
 * AI дают РАЗНЫЕ столбцы Марса, Меркурия, Юпитера, Венеры и Сатурна — числа
 * «плывут»), а сведена по трём независимым человеческим источникам, которые
 * совпали до последней цифры:
 *   1. «Пхаладипика» Мантрешвары, гл. 13, шлоки 10–11 (через калькулятор
 *      argala.ru — там же орбисы д-ра К. С. Чарака);
 *   2. «Джатака Париджата» + «Сарвартха Чинтамани» (сводка Sri Jagannatha
 *      Center, jyotish-blog);
 *   3. astrojyoti.com — та же таблица трактата.
 * Единственное живое расхождение между трактатами — строка ЛУНЫ (см. ниже).
 *
 * Мритью бхага завязана на аянамшу: числа классики читаются в Лахири. У нас
 * Лахири по умолчанию (выбор Георгия) — совпадает.
 */

/** Порядок столбцов таблицы = знаки от Овна (0) до Рыб (11). */
export type MrityuRow = readonly number[];

/**
 * Критические градусы по грахам. Строка Луны — по «Пхаладипике»
 * (её же показывают калькуляторы и справочники, с ней сверялся Георгий).
 * Манди в списке трактата есть, Гулики нет: это одна из причин, почему
 * школа различает две точки (правка Георгия от 06.08.2026, п.4).
 */
export const MRITYU_BHAGA: Record<string, MrityuRow> = {
  'Лагна':    [1, 9, 22, 22, 25, 2, 4, 23, 18, 20, 24, 10],
  'Солнце':   [20, 9, 12, 6, 8, 24, 16, 17, 22, 2, 3, 23],
  'Луна':     [26, 12, 13, 25, 24, 11, 26, 14, 13, 25, 5, 12],
  'Марс':     [19, 28, 25, 23, 29, 28, 14, 21, 2, 15, 11, 6],
  'Меркурий': [15, 14, 13, 12, 8, 18, 20, 10, 21, 22, 7, 5],
  'Юпитер':   [19, 29, 12, 27, 6, 4, 13, 10, 17, 11, 15, 28],
  'Венера':   [28, 15, 11, 17, 10, 13, 4, 6, 27, 12, 29, 19],
  'Сатурн':   [10, 4, 7, 9, 12, 16, 3, 18, 28, 14, 13, 15],
  'Раху':     [14, 13, 12, 11, 24, 23, 22, 21, 10, 20, 18, 8],
  'Кету':     [8, 18, 20, 10, 21, 22, 23, 24, 11, 12, 13, 14],
  'Манди':    [23, 24, 11, 12, 13, 14, 8, 18, 20, 10, 21, 22],
};

/**
 * Вторая строка Луны — «Джатака Париджата» / «Сарвартха Чинтамани». Трактаты
 * тут честно расходятся (единственная строка, где это так), поэтому вариант не
 * выброшен: если Луна попала по нему — приложение скажет об этом отдельно и
 * назовёт трактат, а не выдаст один вариант за общий.
 */
export const MRITYU_MOON_PARIJATA: MrityuRow = [8, 25, 22, 22, 21, 1, 4, 23, 18, 20, 20, 10];

/** Какой трактат дал совпадение (важно только для Луны). */
export type MrityuVariant = 'phaladeepika' | 'parijata';

export const MRITYU_SOURCE: Record<MrityuVariant, string> = {
  phaladeepika: 'Пхаладипика',
  parijata: 'Джатака Париджата',
};

/**
 * Орбис в МИНУТАХ дуги — ОДИН ГРАДУС до и после точки, для всех точек одинаково.
 *
 * Правка астролога 2026-08-07 (раунд 2, §3), источник его же — sudarshana.ru:
 * «действие мритью-бхаги распространяется на нахождение планеты на расстоянии
 * 1° до и после указанных градусов», и «чем ближе Граха расположена к точному
 * градусу, тем сильнее воздействие». Отсюда же — подсветка красным и жирным.
 *
 * Прежний вариант (д-р К. С. Чарак: лагна 60′, Солнце/Луна/Меркурий 40′,
 * прочие 30′) оставлен ниже: он уже, ловит меньше карт, и если школа вернётся
 * к нему — меняется одной строкой в `mrityuOrbMin`.
 */
export const MRITYU_ORB_MIN_DEFAULT = 60;

/** Узкие орбисы д-ра Чарака — НЕ действуют, оставлены для переключения. */
export const MRITYU_ORB_CHARAK: Record<string, number> = {
  'Лагна': 60, 'Солнце': 40, 'Луна': 40, 'Меркурий': 40,
};

export const mrityuOrbMin = (_name: string): number => MRITYU_ORB_MIN_DEFAULT;

/**
 * Сила воздействия по близости к точке: 1 — точное попадание, 0 — край орбиса.
 * Линейно, как и читает правило («чем ближе, тем сильнее»); шкалу школа не
 * задаёт, поэтому не выдумываем кривых.
 */
export const mrityuStrength = (offMin: number, orbMin: number): number =>
  Math.max(0, 1 - Math.abs(offMin) / orbMin);

/** Порог «жирного» выделения: ближе половины орбиса, то есть 30′ и точнее. */
export const MRITYU_STRONG = 0.5;

export interface MrityuHit {
  name: string;
  /** знак, в котором стоит точка: 0 = Овен */
  signIndex: number;
  /** табличный критический градус */
  degree: number;
  /** фактический градус в знаке */
  deg: number;
  /** отклонение от точки в минутах дуги, со знаком (− не дошла, + прошла) */
  offMin: number;
  /** допуск, в пределах которого считаем попадание */
  orbMin: number;
  /** по какому трактату совпало (различается только у Луны) */
  variant: MrityuVariant;
  /** сила: 1 — точно в градусе, 0 — на краю орбиса */
  strength: number;
  /** ближе половины орбиса — выделяем жирным (правка астролога) */
  strong: boolean;
}

const norm = (x: number): number => ((x % 360) + 360) % 360;

/** Табличный градус мритью бхаги для грахи в знаке. null — грахи нет в таблице. */
export function mrityuDegree(name: string, signIndex: number,
  variant: MrityuVariant = 'phaladeepika'): number | null {
  const row = name === 'Луна' && variant === 'parijata'
    ? MRITYU_MOON_PARIJATA : MRITYU_BHAGA[name];
  if (!row) return null;
  return row[((signIndex % 12) + 12) % 12];
}

/**
 * Попала ли точка в мритью бхагу. Проверяем основной трактат, а для Луны — и
 * второй: если совпало только по нему, вернём попадание с пометкой трактата.
 * Долгота — сидерическая (в тропической эти градусы не читаются).
 */
export function mrityuCheck(name: string, lon: number): MrityuHit | null {
  const l = norm(lon);
  const signIndex = Math.floor(l / 30);
  const deg = l - signIndex * 30;
  const orbMin = mrityuOrbMin(name);
  const variants: MrityuVariant[] = name === 'Луна' ? ['phaladeepika', 'parijata'] : ['phaladeepika'];
  for (const variant of variants) {
    const degree = mrityuDegree(name, signIndex, variant);
    if (degree == null) return null;
    const offMin = (deg - degree) * 60;
    if (Math.abs(offMin) <= orbMin) {
      const strength = mrityuStrength(offMin, orbMin);
      return { name, signIndex, degree, deg, offMin, orbMin, variant,
        strength, strong: strength >= MRITYU_STRONG };
    }
  }
  return null;
}

/** Все попадания среди переданных точек (грахи карты, лагна, Манди). */
export function mrityuHits(points: { name: string; lon: number }[]): MrityuHit[] {
  const out: MrityuHit[] = [];
  for (const p of points) {
    const hit = mrityuCheck(p.name, p.lon);
    if (hit) out.push(hit);
  }
  return out;
}

/** «19°58′ — мритью бхага Марса в Овне (19°), недобор 2′». */
export const mrityuLabel = (h: MrityuHit): string => {
  const off = Math.round(Math.abs(h.offMin));
  const where = off === 0 ? 'точно в градусе'
    : `${h.offMin < 0 ? 'не дошла' : 'прошла'} ${off}′`;
  return `${h.degree}° — ${where}`;
};

/** Насколько сильно звучит попадание — словами, по расстоянию до точки. */
export const mrityuForce = (h: MrityuHit): string =>
  h.strength >= 0.9 ? 'в самой точке — предельно сильно'
    : h.strong ? 'близко к точке — сильно'
    : 'у края орбиса — слабее';
