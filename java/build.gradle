// astra-engine — расчётное ядро Astra на Java (западная школа).
//
// Зависимость эфемерид: Java-порт Swiss Ephemeris (Томас Мак). На Maven Central
// его нет, поэтому берём с JitPack по зеркалу на GitHub. Если зеркало окажется
// недоступным, запасной путь — положить исходники порта в src/main/java
// (лицензия AGPL это позволяет, см. README).
plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'ru.svcode.astra'
version = '0.1.0'

repositories {
    mavenCentral()
    maven { url = 'https://jitpack.io' }
}

java {
    // 17 — работает и на обычной JVM, и в Android (desugaring для java.time не нужен с API 26+)
    toolchain { languageVersion = JavaLanguageVersion.of(17) }
    withSourcesJar()
}

dependencies {
    // эфемериды: порт Swiss Ephemeris на Java
    implementation 'com.github.krishnact:swisseph:master-SNAPSHOT'

    // только для тестов: чтение золотых файлов
    testImplementation 'com.fasterxml.jackson.core:jackson-databind:2.17.2'
    testImplementation platform('org.junit:junit-bom:5.10.3')
    testImplementation 'org.junit.jupiter:junit-jupiter'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

test {
    useJUnitPlatform()
    // золотые файлы и эфемериды ищем по путям, заданным сборкой
    systemProperty 'astra.golden', file('golden').absolutePath
    systemProperty 'astra.ephe', file('../ephe').absolutePath
    testLogging { events 'passed', 'failed', 'skipped'; showStandardStreams = true }
}

publishing {
    publications {
        library(MavenPublication) { from components.java }
    }
}
