# ТЗ (раунд 29, план): геометрия неба и инструменты поиска

Идеи владелицы 2026-07-08 (консультация с ИИ). Здесь — движковые фазы;
контентная графа «Аспекты планет к домам» уже в раунде 28
(`TASK_LIBRARY_MYTHS.md`, блок F).

## 1. «ФИГУРЫ ДНЯ» — конфигурации аспектов ★ключевое
### (название УТВЕРЖДЕНО владелицей 2026-07-08; это задание для Fable —
### архитектура и реализация в следующем раунде, НЕ начато)

Идея: не просто аспекты, а ФИГУРЫ (скрытая геометрия). В натале — классика;
изюминка владелицы — ТРАНЗИТЫ: фигура, собирающаяся из транзитных аспектов
(или транзит, ЗАМЫКАЮЩИЙ натальную заготовку — напр. транзитная планета к
натальной оппозиции = Тау-квадрат), выделяется как многоплановое событие —
«динамика и вау-обстоятельства».

### Реестр фигур владелицы (ПОДТВЕРЖДЁН полностью 2026-07-08, 15 фигур)
Мажорные аспекты, орбисы движка. Порядок реестра, ДЕТЕКЦИИ и ОПИСАНИЙ —
строго от простых к сложносоставным (правило владелицы). Каждая фигура
задана КАНОНИЧЕСКИМИ позициями (долготы с точностью до поворота/зеркала) —
из них однозначно следуют рёбра И «пустые» пары.

Треугольники (3 планеты):
1. **Косой парус** — [0,60,180]: 1☍ + 1△ + 1⚹ (уточнение владелицы: это
   ТРЕУГОЛЬНИК, не вариант паруса).
2. **Большой тригон** — [0,120,240]: 3△.
3. **Тау-квадрат** («бумеранг») — [0,90,180]: 1☍ + 2□; вершина в двух □.
4. **Бисекстиль** — [0,60,120]: 1△ + 2⚹; вершина в двух ⚹.
   ✔ Проверка полноты: мажоры замыкаются в треугольник ТОЛЬКО этими 4
   способами (120·3=360; 180=90+90; 120=60+60; 180=60+120) — список
   математически исчерпывающий.

Четырёхугольники:
5. **Большой крест** — [0,90,180,270]: 2☍ + 4□ (содержит 4 тау-квадрата).
6. **Повозка (прямоугольник)** — [0,60,180,240]: 2☍ + 2△ + 2⚹ (содержит
   4 косых паруса).
7. **Парус** — [0,120,180,240]: 1☍ + 3△ + 2⚹ = большой тригон + планета в
   ☍ к вершине (содержит тригон, бисекстиль, 2 косых паруса). Полный граф.
8. **Колыбель** — [0,60,120,180]: 1☍ + 2△ + 3⚹ (содержит 2 бисекстиля,
   2 косых паруса). Полный граф.
9. **Трапеция** — [0,30,90,120]: 1△ + 2□ + 1⚹; △∥⚹, их концы соединены
   квадратами; пустые пары = 30°.
10. **Лира** — [0,90,210,300]: 1△ + 2□ + 1⚹; пустые пары = 150°.
    ⚠ Трапеция и лира — ОДИНАКОВЫЙ состав рёбер: конфигураций {△,⚹,2□}
    математически ровно две, различаются ТОЛЬКО пустыми парами (30° против
    150°) — детекция обязана их проверять.

Пятиугольники:
11. **Конверт** — [0,60,180,240,300]: 2☍ + 4△ + 4⚹ = повозка + бисекстиль
    на грани трина; УЧИТЫВАЮТСЯ ВСЕ ТРИНЫ (указание владелицы 2026-07-08:
    вершина клапана даёт ещё 2△ к дальним углам — они часть фигуры).
    Полный граф.
12. **Мороженое (пентагон)** — [0,30,60,90,120]: 1△ + 2□ + 3⚹ = трапеция +
    бисекстиль от грани трина; пустые пары = 30°.
13. **Кораблик** — [0,90,150,210,300]: 1△ + 2□ + 3⚹ = лира + бисекстиль от
    грани трина; пустые пары = 150° (двойник мороженого — различие снова
    только пустыми парами).
14. **Бриллиант** — [0,60,120,180,270]: 1☍ + 2△ + 3⚹ + 2□ = ☍ делит фигуру:
    с одной стороны колыбель, с ДРУГОЙ — тау-квадрат (2□ к концам ☍).
    ⚠ Вершина тау на ТОЙ ЖЕ стороне не даёт лишних аспектов — рёбрами не
    отличить, обязательна явная проверка стороны.

Шестиугольник:
15. **Щит Давида** — [0,60,120,180,240,300]: 6⚹ + 6△ + 3☍ (полный граф) =
    2 больших тригона в противофазе; «3 оппозиции внутри» ✓ (содержит
    тригоны, повозки, паруса, колыбели, конверты…).

### Логика композиции (требование владелицы)
- Некоторые фигуры СОДЕРЖАТ другие — реестр хранит поле `contains:
  [имена под-фигур]`; детекция идёт снизу вверх: сначала все простые
  (3-планетные), затем составные СОБИРАЮТСЯ из найденных простых + недостающих
  рёбер (не переискивать с нуля — дёшево и даёт готовую декомпозицию).
- Выдача: показывается САМАЯ КРУПНАЯ фигура, её под-фигуры не дублируются
  отдельными карточками, а раскрываются внутри («состоит из: большой тригон
  Луна-Венера-Юпитер + …»).
- Трактовка фигуры строится от простого к сложному: сначала смысл под-фигур,
  затем что даёт их совмещение (тексты — отдельный контент-блок агентами,
  как раунд 28).

### Архитектура (ложится на существующий движок идеально)
- Фигура = набор рёбер-аспектов. Аспект в приложении — ИНТЕРВАЛ, значит
  **окно жизни фигуры = пересечение интервалов всех её рёбер** (вход
  последнего ребра → выход первого). Философия «аспект-окно» сохраняется.
- Реестр data-driven (как ASPECTS): { имя, арность, мультимножество рёбер,
  роль вершин, contains }. Валидация реестра тестом: у каждой фигуры число
  рёбер = C(арность,2) минус разрешённые «пустые» пары (пары без аспекта —
  часть определения, тоже в реестре).
- Поиск: сначала 3-планетные по маске (C(11,3)=165), составные — сборкой из
  простых; режимы: натал (снимок static.ts), транзит×транзит (день),
  смешанный транзит×натал в картах (транзит ДОСТРАИВАЕТ натальную заготовку —
  самый «вау»-режим, приоритет).
- UI «Фигуры дня»: полигон фигуры в колесе (подсветка всех рёбер разом,
  выделение по правилу «сам элемент, не рамка»), карточка с окном
  сборки-распада и декомпозицией; опц. уведомление «сегодня собирается …».
- Этапность реализации: (а) реестр+детекция натала с тестами на синтетических
  картах → (б) транзитные окна → (в) смешанный режим → (г) UI → (д) тексты.
- Верификация НА РЕАЛЬНОМ НЕБЕ (требование владелицы 2026-07-08): при
  проверке многоугольников прогнать сканер по эфемеридам и прислать ей ДАТЫ,
  когда такие фигуры реально стояли на небе.

## 2. Цепочки диспозиторов (формула — готова)

Формула детерминированная, ИИ для построения НЕ нужен (нужен только для
трактовки): **каждая планета → управитель знака, в котором она стоит**
(система владелицы = `SIGN_RULER` в `dispositorLore.ts`), повторять, пока не
придём к планете в СВОЁМ знаке (финальный диспозитор, «царь цепочки») или к
циклу взаимной рецепции (соправители). Солнце/Луна/Раху/Кету ничем не
управляют → они всегда истоки стрелок, в них никто не «впадает»; в графе
максимум 6 узлов-управителей (Марс, Венера, Меркурий, Юпитер, Сатурн, Уран).
- Расчёт тривиален по natal-позициям; визуализация — цепочка глифов
  «X → Y → Z ⟲» или дерево.
- Трактовка композиционная: ребро «архетип X служит архетипу Y (бога знака)»,
  финал — «вся карта стекается к Z». Промт для ИИ-чата: перечислить цепочку
  богами + попросить бытовой разбор «кто на кого работает».

## 3. Описания фаз ретроградности (стоит делать — дёшево)

Ретро уже показывается (чипы ℞ + колесо); станции уже в TODO уведомлений
(`astra-notifications-overhaul`). Добавить контент: фазы × планеты =
тень-до (планета впервые проходит будущий ретро-отрезок) → станция R →
ретро-ход → станция D → тень-после. Ретроградят Меркурий…Нептун (7 планет;
Солнце/Луна — нет; узлы истинные — почти всегда ретро, отдельный текст про
директные узлы как редкость). ~7×5+1 ≈ 36 текстов + привязка к событиям
станций в движке. Библиотека: графа «Ретроградность» или внутрь архетипов.

## 4. Аспекты планет к куспидам домов — движковая часть

Сейчас: куспиды СЧИТАЮТСЯ (swe_houses, 10 систем, в колесе карт), но аспекты
к ним НЕ считаются нигде, орбисов у куспидов нет. Сделать:
- Куспид = точка; аспект планета→куспид той же математикой разницы долгот
  (`static.ts` — снимок для натала; окна для транзитов к натальным куспидам).
- Орбис куспида — отдельная настройка (дефолт 1°, как у планет; пара
  планета×куспид — больший из двух). Куспиды движутся ~1°/4мин реального
  времени рождения — предупреждение при неточном времени (unknownTime →
  дома и так отключены, slowOnly).
- Показ: в карточке карты список «аспекты к куспидам» + трактовка из блока F
  (`planetCuspLore.ts`) — контент уже будет готов в раунде 28.

## 5. Поиск планеты в градусе («Венера в 27°56′ Льва, 2020–2030»)

Возможно, и точно: целевая долгота = знак×30 + градус + минуты; сканер шагает
по эфемеридам и уточняет корень (та же техника root-finding, что для точных
аспектов). Планета может проходить точку 1 или 3 раза за цикл (директ-ретро-
директ) — выдаём ВСЕ прохождения с датой и временем в поясе пользователя.
Диапазон лет любой в пределах файлов эфемерид. UI: мини-инструмент «Поиск
градуса» (планета, знак, градус°минута′, годы от/до → список дат; тап по
дате → открыть день). Дёшево: один новый модуль движка + маленькая шторка.

## Порядок
1→(вопрос владелице: список фигур) реестр+движок фигур; 2 цепочки; 5 поиск
градуса; 4 аспекты к куспидам; 3 ретро-тексты (можно контент-агентом раньше).
