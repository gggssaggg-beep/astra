# HISTORY — детали, сверки, раунды правок

Вынесено из `CLAUDE.md`, чтобы не держать в контексте постоянно. Здесь — обоснования
решений, цифры сверок движка, детали астероидов/TNO и changelog по раундам.

## Движок — статус выбора (2026-06-27): B1 подтверждён полностью
Capacitor + Swiss Eph WASM. Движок — пакет **swisseph-wasm** (emscripten-сборка swe
2.10.03), НЕ @swisseph/browser (отвергнут: сломанный бандл + нет API затмений/Сароса).
Спайк-сверки против питон-эталона:
- позиции (`spike/verify_wasm.mjs`, 10 объектов × 6 дат) — до **0.0000″**, скорости
  до 2·10⁻⁸°/сут, JD идентичны;
- времена точных аспектов (`spike/verify_aspects.mjs`, 23 события) — макс. **0.49 сек**
  при допуске ≤1 мин;
- затмения и номера Сароса §10.1 (`spike/verify_eclipses.mjs`, 10 событий) — серия,
  член серии, тип и время максимума совпали (**Δt ≤ 0.4 сек**).
✅ Спайк движка закрыт. B2 (Chaquopy + pysweph) — фолбэк, не нужен. B3 — нет.

## Сверка с эталоном (обязательна перед UI)
Папка `engine_reference/` — работающий код на `pyswisseph/pysweph`. Любой новый
движок обязан давать ИДЕНТИЧНЫЙ результат: долготы до 0.01°, времена точных аспектов
до минуты.

**РЕЗУЛЬТАТ (2026-06-27).** Спайк в `spike/`. `dump_reference.py` дампит эталон
(pysweph 2.10.03, фактически Moshier — файлов эфемерид нет) в `reference_dump.json`
(позиции, аспекты, затмения+Сарос). Три verify-скрипта гоняют **swisseph-wasm**
(Moshier) через общий `engine.mjs` и сверяют: `verify_wasm.mjs` (позиции),
`verify_aspects.mjs` (времена аспектов), `verify_eclipses.mjs` (затмения §10.1).
Даты позиций: 2000-01-01, 2010-09-09, 2020-12-21, 2023-03-02, 2025-01-01, 2026-06-27.
Итог: позиции до **0.0000″**, аспекты ≤ **0.49 сек**, Сарос **10/10**. ✅

Прогон: `cd spike && npm install && npm run verify`. Пересборка эталона —
`../.venv/Scripts/python.exe dump_reference.py` (через `npm run dump:ref` путь к
python в cmd не резолвится — звать python напрямую).

**Попутно исправлен баг ЭТАЛОНА** (`engine_reference/daily_aspects_astro.py`,
`_exact_time`): точное время не считалось для СОЕДИНЕНИЙ и ОППОЗИЦИЙ (там
`|sep|-target` лишь касается 0/180 и не меняет знак → было `exact_time=None` для всех
☌ и ☍). Теперь для ☌ ищем ноль знаковой `angdiff(lon1,lon2)`, для ☍ — `angdiff(delta,
180)`. Проверено: Юпитер–Сатурн 2020-12-21 → 18:20:37 UTC (=0°29′10″ Водолея).
Оставшиеся `None` у ☌ — легитимные «близкие прохождения» (мин. разница >0, точного 0°
нет, напр. Нептун–Кету 0.25°).

**Окно орбиса (требование астролога).** Аспект — ИНТЕРВАЛ, не «весь день». В записи
аспекта `begin_time`/`end_time` (вход/выход орбиса) рядом с `exact_time`
(`_orb_distance`/`_orb_edge` в эталоне). Окно может выходить за сутки: Юпитер–Сатурн ☌
2020-12 — окно 12→30 дек (18 дней); Луна–Сатурн ⚹ — вход 21 дек 21:32, выход 22 дек
01:33 (через полночь).

## Выбор пакета WASM (2026-06-27): swisseph-wasm, не @swisseph/browser
Взят **swisseph-wasm** (prolaxu), а НЕ @swisseph/browser@1.1.1. Причина: @swisseph
отдаёт у затмений только времена (нет `*_eclipse_how`/`*_where` в экспортах) → §10.1
(Сарос) невыполним; плюс dist-бандл сломан (CJS-куски в ESM, ссылки свёрнуты в
`(void 0)` — жил на заплатке patch_vendor.mjs). swisseph-wasm — штатный emscripten-glue
(заплаток НЕ нужно), экспортирует ПОЛНЫЙ низкоуровневый API (sol/lun eclipse
when/where/how) → Сарос берём через `Module.ccall` (см. `spike/engine.mjs`). Бонус: в
пакете есть полные файлы эфемерид (`swisseph.data`, ~12 МБ). Минусы: пакет молодой
(v0.0.5, один автор) и GPL-3 — но это лишь сборка канонического C-исходника Swiss Eph.
Сарос: серия=`attr[9]`, член=`attr[10]` из `*_eclipse_how`; для солнечных сперва
`sol_eclipse_where` (точка максимума), иначе how вернёт 0.

## Доп. объекты и догрузка (астероиды/TNO) — проверено 2026-06-27
Режим SWIEPH. Код объекта: астероид = `SE_AST_OFFSET(10000) + номер`. Расчёт — обычный
`swe_calc_ut(jd, ipl, FLG_SWIEPH|FLG_SPEED)`.
- **В бандле `swisseph.data`** (смонтирован в `/sweph`): `sepl_18`, `semo_18`,
  `seas_18` + таблицы. Считаются СРАЗУ: Хирон(15), Фолус(16), Церера(17), Паллада(18),
  Юнона(19), Веста(20), Лилит ср.(12)/ист.(13). Спайк: ✅ все.
- **Чего нет в бандле:** индивидуальные файлы TNO. Имя файла даёт сама библиотека в
  тексте ошибки: Эрида → `s136199.se1` (длинный, 3000 BCE–2999 CE) или `s136199s.se1`
  (короткий 1500–2100); номера ≥100000 начинаются с `s`, <100000 — с `se`. Файл ОБЪЕКТА
  кладётся в подпапку `astN` (N=номер/1000): Эрида → `/sweph/ast136/s136199.se1`.
- **Догрузка во время работы РАБОТАЕТ** (проверено): у emscripten-модуля доступна
  виртуальная ФС — `swe.SweModule.FS.writeFile('/sweph/astN/<файл>.se1', bytes)`, затем
  `calc_ut` сразу видит объект. Спайк `spike/probe_extra_objects.mjs`: Церера на 1000 г.
  падала «нет `seas_06.se1`» → после записи файла в ФС посчиталась.
- **РЕШЕНО: набор астролога ВШИТ в APK** (оффлайн-первый; ~15–20 КБ на объект, Эрида
  171 КБ — итого ~0.3 МБ). Астролог прислала 19 файлов (папка `sw/`), разложены в
  `ephe/astN/`: Хаос(19521), Варуна(20000), Иксион(28978), Хуйя(38628), Квавар(50000),
  2002 UX25(55637), 2003 VS2(84922), Седна(90377), Оркус(90482), 2004 GV9(90568),
  Салация(120347), Хаумеа(136108), Эрида(136199), Макемаке(136472), Варда(174567),
  2003 AZ84(208996), Гонгун(225088), 2002 MS4(307261), Лелеакухонуа(541132). Все, кроме
  Эриды — «короткие» (1500–2100). При сборке копируются в ассеты, на старте пишутся в
  `Module.FS`. Имена/коды — data-driven (скан `ephe/ast*/`), см. `dump_swieph.py`.
- **Источник файлов TNO:** Dropbox Алоиса (`long_ast/astN/` длинные, `all_ast/astN/`
  короткие) — но это JS-страница, СКРИПТОМ (curl) НЕ качается (отдаёт HTML-обёртку;
  настоящий файл начинается со слова `SWISSEPH`). Файлы взяты ГОТОВЫЕ у астролога. Ядро
  (`sepl_18/semo_18/seas_18`) — на GitHub `aloistr/swisseph/ephe` (в `ephe/` для
  питон-эталона; в APK не нужно — внутри `swisseph.data`).

**СВЕРКА SWIEPH↔SWIEPH (2026-06-27).** `spike/dump_swieph.py` (pysweph с файлами из
`ephe/`) ↔ `spike/verify_swieph.mjs` (swisseph-wasm; те же файлы пишутся в его ФС →
читают идентичные данные). **38 объектов** × 6 дат = **228 значений**, 0 ошибок: макс.
ошибка долготы **0.069″** (≈2·10⁻⁵°, худший — Марс; TNO ещё точнее), скорости
4·10⁻⁷°/сут — внутри допуска с запасом. ✅ Прогон: `cd spike &&
../.venv/Scripts/python.exe dump_swieph.py && npm run verify:swieph`.

Пример «почему числа зависят от времени»: Великое соединение Юпитер–Сатурн 2020-12-21.
На 12:00 UTC Сатурн 0°27′ Водолея, но ТОЧНОЕ соединение — в 18:20:36 UTC, и тогда обе
планеты ровно в **0°29′10″ Водолея**. Снимок на полдень ≠ момент точного аспекта.

## Доп. пожелания астролога — раунд 3 (2026-06-27)
Поверх ЗАДАНИЯ §3.5–3.7:
- **Архетипы божеств греческой мифологии — на КАЖДУЮ планету** (не только на сигнатуру
  аспекта). У объекта — редактируемый текст-архетип бога. Связать с §3.7.
- **Бортовой журнал = заметки/наблюдения** (§3.5). Из записи — ИИ-анализ «на совпадение
  с мифом» (модель сопоставляет наблюдение с архетипом). ИИ только по запросу, на ключе.
- **Виды журнала:** группировка/фильтр по периоду (день/неделя/месяц) + выборка по
  конкретной планете ⇒ запись тегируется датой и объектом(ами).
- **Функция СРАВНЕНИЯ заметок:** выбрать 2+ записи и сравнить.
- **Ввод своего токена Claude** — ключ только в защищённом хранилище. Перед ИИ-частью
  свериться со справкой по Claude API (skill `claude-api`).

## Раунд 4 — отзывы астролога (2026-06-30, реализовано)
Веб-часть, проверено `npm run check` + `node test/parity.mjs` + `npm run build`:
- **Заметки «не сохранялись»** — баг реактивности Svelte 5: db-коллекции возвращают ТУ
  ЖЕ ссылку массива, `put`/`remove` мутируют на месте → присваивание не триггерит
  `$state`/`$derived`. ПРАВИЛО: в компонентах брать `db.x.all().slice()`.
- **Единый часовой пояс:** сутки/аспекты/события — на 00:00 ВЫБРАННОГО пояса
  (`zonedDayStartUTC`/`todayCivil`/`fmtDayMid` в `lib/format.ts`).
- **Колесо/положения — на «настоящий момент»** (тик раз в 60с, `DayScreen` `snapshot`);
  аспекты — за все сутки (интервалы).
- **Порядок аспектов** по удалённости от Солнца (`SUN_RANK`/`sunRank`, `constants.ts`);
  в карточке — ДАТЫ входа/выхода.
- **Орбис по КАЖДОМУ объекту** (`Settings.orbs`), для пары БОЛЬШИЙ из двух; резолвер
  `orbResolver` (`models.ts`); `aspectsOn` принимает число ИЛИ функцию-резолвер.
- **Нижняя панель** (Дата/Журнал/Библиотека/Чат/Настройки); **Библиотека** вынесена в
  `LibrarySheet` + `InterpretationsSheet`.
- **Свайп-вниз закрывает шторку** + блок скролла фона — общий экшен `lib/sheet.ts`.
- **Настройки сгруппированы**; «Крупный шрифт» (`Settings.largeFont`).
- **Геолокации НЕТ** (деанона нет): пояс из `Intl...timeZone`. Форум — НЕ делать.
- **OTA — Capgo** (`@capgo/capacitor-updater`, ручной режим `autoUpdate:false`).
  `lib/ota.ts`: старт → `notifyAppReady()` → fetch `apk/latest.json` → если версия
  новее → `download` + `next`. CI зипует `dist`→`apk/bundle.zip` + пишет `latest.json`.

## Раунд 5 — отзывы астролога (2026-07-01, реализовано)
Проверено `npm run check` (0/0) + parity + build + `npx cap sync android` (5 плагинов).
- **ГЛАВНОЕ — данные не сохранялись после перезапуска.** На Android полагались только на
  `localStorage` WebView (не переживает перезапуск/смену origin при OTA). ФИКС:
  durable-стор `@capacitor/preferences` в `lib/db.ts` (`scheduleNativeSave()` пишет,
  `hydrate()` на старте поднимает, await до UI). localStorage — быстрый кэш.
- **Архетипы ВШИТЫ** (`lib/lore.ts` `PLANET_LORE`/`defaultArchetypes`); `hydrate()`
  досевает недостающие, не затирая правки.
- **Обучалка:** приветствие первого запуска (`ui/Welcome.svelte`, `WELCOME` в `lore.ts`,
  флаг `Settings.seenWelcome`); тык по символу в колесе → разбор (`ui/InfoSheet.svelte`
  + `WheelInfo`, контент `PLANET_LORE`/`ASPECT_LORE`).
- **Попап аспекта** (`ui/InterpretationSheet.svelte`): убран блок «Трактовка»; заметка +
  «Обсудить аспект с Claude» (`ondiscuss` → `App.openChat`).
- **#13 Уведомления нативно** (`@capacitor/local-notifications`, `lib/notifications.ts`):
  ежедневная сводка + момент точного аспекта планета–планета. `syncNotifications(...)`.
- **#5/#12 экспорт/импорт на Android** (`lib/backup.ts`): `@capacitor/filesystem` +
  `@capacitor/share`; импорт — `<input type=file>`.
- **#7 «Подгрузить миф (Claude)»** в `ArchetypesSheet` (`chat.loadDeityMyth`).
- **Скорость прокрутки:** убран `backdrop-filter: blur` с потоковых `.glass`-карточек;
  blur только на оверлеях (`.frost`). Космос+звёзды — один неподвижный `fixed`-слой.
- **Дата «завтра»/«сегодня»:** `App` передаёт `today={todayCivil(settings.tz)}` в
  `DateSheet` (считалось по поясу устройства, а не выбранному).

## Раунд 6 — доделки + фикс сохранения (2026-07-01)
Проверено `npm run check` (0/0). ⚠️ keystore/`build.gradle` — НАТИВНЫЕ правки, нужна
свежая сборка APK через CI; веб-правки едут по OTA.
- **✅ Фиксированный keystore.** `signingConfigs.debug` в `app/android/app/build.gradle`
  → закоммиченный `astra-debug.keystore`; CI (`build-apk.yml`) генерит его через
  `keytool` один раз и коммитит (`git add -f`), дальше переиспользует → подпись APK
  стабильна → обновление ставится ПОВЕРХ без потери данных. `.gitignore` — точечное
  исключение из `*.keystore`. ⚠️ РАЗОВО при переходе на новый ключ данные слетят
  (подпись сменилась с прежней рандомной): экспорт → переустановка → импорт, дальше без
  потерь.
- **✅ Настоящий баг «данные не сохраняются»** (`lib/db.ts`): `serialize()` пишет ОБЁРТКУ
  `{app,schema,savedAt,data:{…}}`, а `loadLS()` читал её как ПЛОСКУЮ → коллекции
  поднимались ПУСТЫМИ. Ввёл `payload()` (достаёт `.data`), применил в `loadLS` и `adopt`.
- **✅ Автосохранение переписки с Claude** (`ui/ChatSheet.svelte`): после каждого ответа
  разговор пишется в журнал сам — ОДНА запись на разговор (upsert по `noteId`). Из
  карточки аспекта (`App.openChat(seed, source)`) — тег объектов+сигнатуры. Перенос на
  другой телефон — штатным экспортом/импортом; P2P не делаем.
- **✅ Свайп-закрытие шторок не такое резкое** (`lib/sheet.ts`): «потянуть-закрыть»
  смотрит на ближайший ВНУТРЕННИЙ скролл под пальцем; порог 90→130 px.

# Хроника раундов (перенесено из CLAUDE.md 2026-07-11; правило: CLAUDE.md ≤ 200 строк)

## Сделано 2026-07-02 (раунды 7–10, дизайн влит в main — CI/OTA идут с main!)
- OTA-откат починен (`lib/ota.ts`: notifyAppReady сразу в main.ts, версия из
  `current()`, НЕ localStorage-маркер; appReadyTimeout 30с).
- Чат: модель **`claude-sonnet-5`** — РЕАЛЬНАЯ (вышла 2026-07-01), НЕ «исправлять».
- Уют-раунд: хаптика `lib/haptics.ts`, ретро-℞ (чипы+колесо), фаза Луны, живой
  «Рассвет», направленное листание дня, пауза Starfield при шторках (перф).
- **GlowCard = единая обводка всего** (selected / onactivate «обвести→открыть»);
  выделенный аспект = ЛИНИЯ в колесе (selectedSignature), НЕ рамка карточки.
- Трактовки возвращены (блок в шторке аспекта + строка на карточке); «неявный»
  ввод `.seamless` + `use:autogrow`; обучалка журнала (`seenJournalHelp`);
  сравнение заметок §3.5 (⇄ в журнале) — есть; шторки: порог 180px/смах,
  html-замок скролла, закрытие «на пункт выше».
- **СООБЩЕСТВО** (Supabase): `supabase/schema.sql`, `lib/community.ts` (URL+
  publishable-ключ ВШИТЫ — не секрет, RLS), `ui/CommunitySheet.svelte`; вход
  Google через системный браузер + deep link `astra://auth` (intent-filter,
  плагины @capacitor/app+browser → APK от 02.07 вечера+). Если «не работает»:
  проверить у владелицы Google-provider/Redirect URLs/schema.sql/Test users.

## Сделано 2026-07-02 вечер (раунд 11, влит в main → CI)
- **OTA-расклин:** бандл со статусом error (скачался→не запустился→откат) больше
  НЕ переиспользуется — удаляется и качается заново (симптом «установлено 21,
  в манифесте 22, не берёт»); кнопка «⟳ Обновить сейчас» = `reload()` (перезапуск
  из недавних не всегда убивает процесс → `next()` не применялся).
- **Колесо — жёсткое правило: выделяется САМ элемент, не рамка.** Тап по знаку
  зодиака → подсветка символа + разбор знака (`SIGN_LORE`, 12 текстов); тап по
  планете → глиф светится; системный tap-highlight WebView погашен глобально.
- Карточка аспекта: GlowCard «обвести→открыть»; просмотренный аспект ОСТАЁТСЯ
  выделенным после закрытия (selSig в App, сброс — выбором другого).
- Нижнее меню: Журнал·Библиотека·Сообщество·Настройки (Дата — тап по дате в
  шапке; Чат — пункт Библиотеки). Возврат из Сообщества — откуда открыли.
- Шторки: замок фона `position:fixed` + восстановление скролла (раньше
  `overflow:hidden` на html сбрасывал ленту в ноль — «не возвращает на место»).
- Перф: кэш аспектов/событий дня В МОДУЛЕ DayScreen (свайпы не пересчитывают
  WASM), drop-shadow только у выбранной линии, scroll-нить целиком на transform.
- **Статус-бар:** `env(safe-area-inset-*)`=0 в Android WebView → везде
  `var(--safe-top/--safe-bottom)` (инжектирует ядро Capacitor 8 SystemBars,
  фолбэк env()); иконки часов по теме приложения (SystemBars.setStyle).
- Настройки: время сводки не режется; «Запросить разрешение» + счётчик
  запланированных уведомлений; легенда цветов под колесом; архетипы — свёрнутый
  текст целиком.

## Сделано 2026-07-03 (раунды 12–13, в main → бандл 0.1.0.29)
- «Квадратик» на тап = ФОКУС-РАМКА: хит-зоны колеса без role/tabindex, :focus
  outline погашен глобально. Тап по ЛИНИИ подсвечивает линию (effSig из
  selectedInfo). Все шторки выезжают СНИЗУ (translate 100%→0, 0.34s).
- Уведомления: requestPermissions НЕ дёргается при уже выданном разрешении
  (висло «Отправляю…»); таймауты на все нативные вызовы с именем шага.
- Вибро: НАТИВНЫЙ @capacitor/haptics (короткий navigator.vibrate игнорируют
  прошивки) + фолбэк ≥20мс — нужен APK с плагином.
- Сообщество: из Библиотеки убрано (только нижнее меню). «Доступ заблокирован»
  = Google redirect_uri_mismatch: в её OAuth-клиент НЕ вписан
  https://vbaysgzdvdyljlwlnivq.supabase.co/auth/v1/callback. Публичного API у
  Google для этого НЕТ (только руками в консоли). Обход отгружен: вход по
  ссылке на почту (signInWithOtp → deep link). Дефолтный SMTP Supabase шлёт
  только членам проекта — коллеге нужен Google-фикс или custom SMTP.
- Архетипы: autogrow с перезамерами + field-sizing: content.

## Сделано 2026-07-04 (большая сессия, всё в main → OTA)
- **УВЕДОМЛЕНИЯ ПОЧИНЕНЫ**: корень — динамический `import()` плагина висел в
  офлайн-WebView; перевели на статический импорт (`lib/reminders.ts`, порт
  принципа MENS: один канал `care`, без кастомной иконки, журнал шагов). Дайджест
  аспектов в сводке; тап по уведомлению → день+выделенный аспект; flush перед OTA.
- **Скролл/шторки**: scroll-нить встроена в общий экшен `use:bottomSheet` (нить
  только у верхней шторки, до низа; правый бегунок скрыт); «Назад» закрывает
  шторку; единая высота `section.sheet.glass` 90vh; блёстки сквозь всё приложение.
- **Сообщество**: удаление (автор/админ-по-email), страница пользователя,
  подписки на людей (`follows`) и ветки (`thread_subs`), колокольчик уведомлений
  (таблица `notifications`+триггеры), метка «💬 есть обсуждение» на аспектах.
  Нужен прогон обновлённого `supabase/schema.sql`.
- **Выбор шрифта** (10 вариантов с кириллицей, `lib/fonts.ts`); год в дате шапки.

## Сделано 2026-07-04 вечер (СИНАСТРИЯ, в main → OTA; архитектура Fable, код субагенты Opus)
- **Совмещённые карты = ТОЛЬКО синастрия** (владелица: «с композитами не
  работаю»). Математика композита (`compositeChart`) написана/оттестирована и
  ЗАКОНСЕРВИРОВАНА в движке — в UI её НЕТ, не удалять (пригодится наталу).
- Ядро: `Person` в `models.ts` (дата/время/пояс места/место — опц., готово к
  домам), коллекция `db.people` (export/import + durable). Момент рождения —
  `zonedTimeUTC` в `format.ts` (DST-безопасно; `unknownTime` → полдень пояса).
- Статичный аспект ≠ транзитный: `engine/static.ts` — СНИМОК (разница углов, без
  окна/времени), НЕ переиспользует `AspectRecord`. `synastryAspects` (полный
  A×B, орбис пары = больший из двух, p1 всегда из A); `staticKey` для линий.
- UI: `ui/SynastrySheet.svelte` (люди CRUD → выбор двух → карта), пункт
  «Синастрия» в Библиотеке, `showSynastry` в App. Двойное кольцо в `Wheel.svelte`
  (props `positionsOuter`/`staticAspects`/`selectedStaticKey`/`onstatictap`; A
  внутри, B снаружи; транзитный режим не тронут), `StaticAspectRow.svelte`.
- Тест `test/static.mjs` (`npm run test:static`, чистый Node без WASM) 20/20;
  parity транзитов не сломан; svelte-check 0/0. Дома — отдельная фаза 3.

## Сделано 2026-07-08 (раунд 28: библиотека мифов, в main → CI)
- **684 трактовки** агентами (ТЗ `docs/TASK_LIBRARY_MYTHS.md`; урок: партии
  3–4 текста на Edit — крупные генерации ТЕРЯЛИСЬ на лимитах/обрывах):
  `houseLore.ts` переписан через мифы богов (144), «Планеты в знаках» (132),
  «Планеты в домах» (132), «Управители домов» (144, ключ
  «ЗнакКуспида|ДомПоложения»; система владелицы = `signMyths.ruler` — управитель
  несёт бога знака: Рак→Меркурий, Козерог→Уран, НЕ классика, НЕ «исправлять»),
  «Аспекты планет к домам» (132 — планета аспектирует куспид; справочный,
  аспекты к куспидам движок пока НЕ считает — см. TASK_SKY_GEOMETRY).
- UI: шторки PlanetSigns/PlanetHouses/PlanetCusps (аккордеоны) + Dispositors
  (чипы дом→знак куспида→дом положения), 4 ряда в Библиотеке, THEME вынесен в
  `lib/houseThemes.ts`.
- Колесо в «Картах»: **масштаб прокрутки День/Месяц/Год** (оборот диска ×1/30/365,
  шаг ‹› календарный) — в транзит+натал и тройной.
- env `CLAUDE_CODE_SUBAGENT_MODEL=opus` в `.claude/settings.local.json`
  (просьба владельца — субагенты жёстко на Opus).

## Сделано 2026-07-09 (РАУНД 29 «ФИГУРЫ ДНЯ» — фигуры готовы; архитектура Fable, код Opus)
- **Реестр 15 фигур подтверждён владелицей** (косой парус — ТРЕУГОЛЬНИК;
  +колыбель/лира/мороженое/кораблик). Каждая задана КАНОНОМ (долготы); рёбра И
  «пустые» пары выводятся из канона автоматически. Конверт = ВСЕ трины
  (2☍+4△+4⚹, указание владелицы). ТЗ обновлено (`docs/TASK_SKY_GEOMETRY.md`).
- `engine/figures.ts` — ЧИСТАЯ математика без WASM: `FIGURES` (data-driven,
  простые→сложные), `detectFigures(points, edges)` (общий матчер по канону,
  снизу вверх, `parts` декомпозиция, максимальность), `figureWindow` (окно =
  ПЕРЕСЕЧЕНИЕ интервалов рёбер). Двойники трапеция/лира и мороженое/кораблик
  различаются ТОЛЬКО пустыми парами (30° vs 150°); бриллиант — проверкой
  СТОРОНЫ тау от оппозиции. Тест `test/figures.mjs` (`npm run test:figures`)
  51/51; parity/static не тронуты; svelte-check 0/0; vite build ok.
- **День:** `figuresOnCached` (`lib/dayCache.ts`) → блок «Фигуры дня» в
  `DayScreen` (крупные вперёд, окно сборки-распада). **Карты:**
  `lib/chartFigures.ts` (кольца A/B/T разъединены тегом имени — иначе «Солнце»
  натала и транзита сливались), фигуры ЗАМКНУТЫЕ транзитом (бейдж) в
  transit+натал/тройной; раздел «Фигуры» во всех режимах `ChartsSheet`.
- **Колесо:** новые пропы `figureSigs`/`figureStaticKeys` — подсветка ВСЕГО
  полигона (набор линий, «сам элемент, не рамка»). Карточки `FigureCard`/
  `ChartFigureCard` (тап → полигон + декомпозиция + трактовка).
- **Тексты:** `lib/figureLore.ts` (15× образ+«на практике», бытовой язык).
- **Сканер для владелицы:** `test/scan_figures.mjs` → `docs/FIGURES_SKY_DATES.md`
  (даты реальных фигур; детектор независимо нашёл знаменитый Большой крест
  7–9.08.1999 — самоподтверждение). ⚠ RELEASE: ещё НЕ закоммичено/не в main.

## Сделано 2026-07-09 (РАУНД 29 остаток §2–§5 — весь раунд закрыт; Fable+Opus)
- **§2 Цепочки диспозиторов** `lib/dispositors.ts`: функциональный граф
  (планета→управитель своего знака `SIGN_RULER`, до царя/кольца рецепции);
  UI `ui/DispositorChains.svelte` в натал-карте. Тест `test:dispositors`.
- **§5 Поиск градуса** `engine/degreeSearch.ts` (`findDegreePassages`,
  `degreeToLon`, все 1/3 прохождения с директ/ретро) + шторка
  `ui/DegreeSearchSheet.svelte` в Библиотеке. Тест `test:degree`.
- **§4 Аспекты к куспидам** `lib/cuspAspects.ts` (орбис куспида дефолт 1°,
  пара = больший) + раздел в натал-карте с трактовкой `PLANET_CUSP_LORE`.
  Тест `test:cuspaspects`.
- **§3 Фазы ретро** `engine/retro.ts` (`retroPhase`: тень-до/станция R/ретро/
  станция D/тень-после по станциям и долготам; узлы вибрируют → retro/direct
  без тени) + `lib/retroLore.ts` (композиция: характер планеты × смысл фазы) +
  графа `ui/RetroSheet.svelte` в Библиотеке (фаза «сейчас» движком). Тест
  `test:retro`.
- Все тесты зелёные, parity не тронут, svelte-check 0/0, build ok. ⚠ RELEASE:
  фигуры уже в main (f46a5aa); §2–§5 — коммитятся этой сессией.

## Сделано 2026-07-09 (доработки по запросу владелицы; Fable+Opus)
- **Пункт «Фигуры дня» в Библиотеке** — `ui/FiguresSheet.svelte` (справочник 15
  фигур: состав рёбер, «состоит из», образ+на практике из `figureLore.ts`).
- **Поиск «когда ещё» = НАТИВ+ТРАНЗИT** (жалоба: искал два транзитных тела):
  `engine/aspectSearch.findAspectToPoint` (движущаяся планета к НЕПОДВИЖНОЙ
  натальной долготе). `StaticInterpretationSheet` получил проп `anchor`
  ({lon натальной p1, planet транзитной p2}); `ChartsSheet.openDetail` считает
  anchor во ВСЕХ режимах (натал/синастрия/транзит+натал/тройной) → ищет, когда
  транзитный p2 замкнёт натальный p1. Тест `test:aspectpoint`.
- **Колесо крутится на экране дня** — `DayScreen` onscrub двигает момент снимка
  (оборот=сутки, rAF-дебаунс, сброс «↺ сейчас»); натал/синастрия статичны (без
  времени — не крутятся осознанно).
- **ИИ-промпт с числами для расчётов** — `aiPrompt.ts`: секция «Точные числа»
  (эклипт. долготы 0–360°, скорости °/сут, куспиды), CALC_RULES разрешает ИИ
  СЧИТАТЬ самой (мидпойнты/минорные/дома); wired в `ChartsSheet.chartPromptText`.

## Сделано 2026-07-09 (уведомления: тихое время + режим сводки; Fable+Opus)
- **Тихое время** (по умолчанию 22:00–08:00, меняется в Настройках): точечные
  пинги аспектов и транзитов НЕ шлются в это окно (окно через полночь). Сводка
  приходит по своему расписанию (её пользователь и так задаёт). Чистые функции
  `lib/quiet.ts` (`inQuietHours`/`hmToMinutes`/`localMinutes`) — тест `test:quiet`
  (reminders.ts тянет Capacitor, в Node не грузится → логика вынесена).
- **Режим сводки**: `dailyDigestMode` 'once' (раз в сутки) | 'twice' (утро+вечер,
  время каждой выбирается). Слот-логика: каждая сводка перечисляет аспекты
  [слот → следующий слот), вечерняя забирает НОЧНЫЕ. ID-диапазоны в reminders.ts
  расширены (сводки 1000..1029, аспекты 1030..1129, транзиты 1130..1229).
- Настройки: `quietEnabled/quietFrom/quietTo`, `dailyDigestMode/dailyNotifyTime2`
  в `models.ts` (дефолты) + UI в `DataPanel.svelte`. Ретро-станции в дайджест —
  СДЕЛАНО (ниже).

## Сделано 2026-07-09 (версия на сайте)
- **Сайт показывал голую `0.1.0`** (жалоба владелицы): `pages.yml` НЕ штамповал
  номер сборки (штамповал только `build-apk.yml`). Добавлен шаг штампа в
  `pages.yml`; ОБА воркфлоу теперь берут номер из `git rev-list --count HEAD`
  (нужен `fetch-depth: 0`), поэтому сайт и APK показывают ОДНУ версию для одного
  коммита (раньше — разные `github.run_number` каждого воркфлоу). Скачок 74→179
  (счётчик коммитов > прежнего run_number) — версия только растёт, OTA-безопасно
  (`isNewer` в `ota.ts`). Футер `DataPanel.svelte:436` → «Astra · версия 0.1.0.179».

## Сделано 2026-07-09 (режим «Экономия аккумулятора»)
- **Анализ расхода** (по убыванию): Starfield-блёстки (canvas 30 к/с, 81
  частица, всегда, поверх всего — главный виновник) > аврора-дрейф фона
  (`aurora-drift 52s infinite`) > `backdrop-filter: blur(12px)` на `.frost`/
  `section.sheet` (дорого на мобильном GPU, пока открыта шторка) > мелкие
  `infinite`-пульсации (breathe у карточек/линии) + drop-shadow глоу. Нет
  websocket-подписок Supabase и WebGL.
- **Тумблер** `settings.batterySaver: 'off'|'auto'|'on'` (дефолт `'auto'`),
  сегмент в Настройках → Внешний вид. Механика: App ставит `data-saver='on'`
  на `<html>` (как `data-theme`); `app.css` одним селектором гасит аврору,
  blur, глоу-фильтры, все анимации (мгновенны, как reduced-motion — НЕ
  play-state:paused, иначе шторка застынет за экраном); `Starfield.svelte`
  читает тот же флаг через MutationObserver и не крутит rAF-цикл (live).
- **Авто**: `lib/battery.ts` (`watchLowBattery`, Battery Status API — есть в
  Android WebView/Chromium, нет в Safari → тихо 'не низкий') включает при
  ≤20% и не на зарядке. Эффективно = `mode==='on' || (mode==='auto' && low)`.
- Глубина = «Максимум» (режем всё дорогое). Смягчить = убрать из
  `:root[data-saver='on']` блоки blur/анимаций.

## Сделано 2026-07-09 (ретро-станции в сводке + мягкое закрытие шторок)
- **Ретро-станции как события дайджеста** (жалоба «Нептун → ретроград не
  попадал»): `engine/retro.ts` `stationsBetween(E, planet, from, to)` — развороты
  R↔D по смене знака скорости (шаг 0.5 сут + бисекция); Солнце/Луна/узлы пусто.
  `reminders.ts`: станции недели считаются раз, кладутся в слот сводки, чей
  интервал их накрывает; строка `☿ ℞ ретроград 14:30` идёт ВПЕРЕДИ аспектов.
  Тест `test/retro.mjs` (+2 проверки: Меркурий 14 янв/3 фев 2022).
- **Мягкое закрытие шторок**: ✕ и бэкдроп уже закрывали (у всех 24 шторок есть
  `.backdrop`+`.x`), но РЫВКОМ. Теперь в экшене `lib/sheet.ts` перехват клика по
  `.x/.xbtn/.backdrop` в фазе ЗАХВАТА (гасит родной `onclose`) → лист доезжает
  вниз (`translate -50%,105%`) + бэкдроп гаснет, `onclose` в конце — как свайп.
  Свайп-закрытие не тронуто; кнопка «Назад» закрывает как прежде (мгновенно).

## Сделано 2026-07-09 (вычитка Sonnet + разнос глифов в стеллиуме)
- **Вычитка текстов** 4 агентами Sonnet (параллельно, непересекающиеся файлы;
  правило: интерфейс на «ты», авторские раскладки богов/мотивы знаков НЕ трогать,
  только реальные ошибки). Найдено 10 правок: 7× ошибочное «вы»→«ты»
  (AstrologerSheet, CommunitySheet), `war-режим`→`военный режим` (dispositorLore),
  пропущенная запятая + `режут друг о друга`→`режут друг друга` (pairAspectLore).
  Остальные lore/UI-файлы — чисто (раунд 15 уже прогонял корректуру). Прогонять
  снова при добавлении крупных партий текста.
- **Разнос глифов в стеллиуме** (`ui/Wheel.svelte`): у `placeRing` число радиусов-
  уровней стало параметром `levels`; одиночное (день) кольцо теперь до 5 уровней —
  плотный стеллиум 4–5 планет в пределах ~8° не слипается (раньше 4-я+ планета
  клампилась на самый глубокий уровень и налегала). Двойное/тройное — 3 (места
  меньше). Старт с наибольшего разрыва (граница 0° Овна) уже был.

## Сделано 2026-07-09 (три идеи раунда 29 — в стиле астролога)
- **Тексты ретро расширены**: `PHASE_PRACTICE` (по фазе — «на практике», что
  делать, бытовой «ты») в `retroLore.ts`; `retroText` отдаёт practice, `RetroSheet`
  показывает блок «На практике» (цианová кромка) под «Сейчас».
- **Транзиты к куспидам** (было — только натал-снимок): `lib/transitCuspLore.ts`
  (`transitCuspText` = характер транзитной планеты × тема дома × тон аспекта,
  композиция); `ChartsSheet.transitCuspAsp` = `cuspAspects(transitPos, housesA)`
  в режимах transit+натал/тройной; раздел «🚶 Транзиты к куспидам» (аккордеон).
- **Цепочки диспозиторов → ИИ-чат**: `dispositorPrompt(res, positions)` в
  `dispositors.ts` (бытовой промт с характерами планет-богов, «управители
  авторские — не переправляй на классику»); кнопка «🔮 Разобрать цепочки в чате»
  в `DispositorChains` (проп `onexplain`) → `onchat(seed, selfContained)`.

## Сделано 2026-07-09 (баги фигур + ось узлов)
- **Пустые рамки фигур**: 2 бага показа. (1) `use:reveal` внутри шторок не
  срабатывал (IO мимо transform+своего скролла) → `lib/reveal.ts`: корень IO =
  скролл-контейнер + таймер-фолбэк 700мс (контент не остаётся невидимым нигде).
  (2) `ChartFigureCard` имел класс `.reveal` (opacity:0) БЕЗ `use:reveal` →
  вечно невидим (пустая рамка в Картах); добавлен экшен. [[astra-reveal-invisible-in-sheets]]
- **Ось узлов = не ребро фигуры** (по умолчанию): Раху и Кету всегда ровно 180°
  (геометрия) — это ось, не найденная конфигурация; Луна быстрая → «косой парус»
  на Луне+узлах висел почти всё время (тау/кресты — тоже). `isNodalAxisPair`
  (`engine/figures.ts`); `figuresOnCached`/`chartFigures` фильтруют это ребро при
  `nodalAxis=false`. В картах фильтр — по ОДНОМУ кольцу (Раху/Кету разных карт =
  реальный аспект). Тумблер `Settings.nodalAxisFigures` (дефолт false) в Объектах
  → «Строить фигуры на оси узлов». Проверено: детектор без ребра паруса не даёт.

## Сделано 2026-07-10..11 (марафон: бэклог + правки владелицы + онбординг + иконки; Fable+агенты)
- **Бэклог рутин ЗАКРЫТ ЦЕЛИКОМ**: UX Партии 1–7 (PR#6..#16), ENERGY Б-1..Б-9
  (+'max' при ≤10%), STUDY A–D (глоссарий/курс/тур), SYSTEM SR-3 (digestSlots
  +тест) / SR-4 (ChartsSheet 1213→933: PersonForm/TransitControls/PeopleList).
  Google-вход починен ДЛЯ ВСЕХ (OAuth Production + Site URL; webRedirect в
  community.ts). Шрифт интерфейса ЕДИНЫЙ (выбор из 9 удалён — просьба).
- **Скраб главного колеса как в Картах**: сегмент День/Месяц/Год, дата меняется
  В ШАПКЕ (effectiveDate; scrub-стейт в App, {#key} остался на anchor-date —
  без ремаунта/сброса). Выделение аспектов = тумблер (1 тап выделить+открыть,
  2-й снять); если после скраба выделенный аспект ушёл с карты — сброс.
- **Глифы планет: SVG-перерисовка ОТМЕНЕНА владелицей** («перерисовали плохо»)
  — Unicode-шрифт как был, ретро золотые; НЕ предлагать SVG-планеты снова.
  Знаки Tabler остались + signStyle 'auto' = дефолт (тьма=серебро,
  свет=радуга); «Переливание» (shimmer) убрано из выбора.
- **Иконки интерфейса — свои SVG** (`ui/icons.ts`+`Icon.svelte`, Tabler-стиль,
  currentColor): нижнее меню (книга/люди/искра/шестерёнка), все 15 пунктов
  Библиотеки, колокольчик+карандаш Сообщества. «Открыть карту» — НАД списком.
- **Обучалка**: квиз не «автонажимается» (сброс picked по lessonId), колесо
  ПРЯМО в вопросе (quiz.wheel + glyphStyle-стор), `LessonVisual.svelte` —
  иллюстрации во всех 8 уроках (карточки аспектов с углами, линейка орбиса
  119–121°, мок окна вход→точно→выход, стихии/кресты, фазы ретро, ось узлов,
  мини-фигуры, колесо домов) — символы те же, что в приложении.
- **ОНБОРДИНГ — все 8 пунктов** (бриф `docs/TASK_ONBOARDING.md`): Welcome-мастер
  «Добавь себя» (встроенный PersonForm → db.people + transitSelfId), разовая
  пульсация «?» (старт ПО ЗАКРЫТИЮ Welcome — иначе сгорала за оверлеем), пустой
  день «не поломка», разбор первой карточки (seenAspectCardHelp), бейдж-точка
  тура на Настройках + раздел «Обучение» выше, строка «необязательная соцсеть»,
  карточка-контекст перед запросом уведомлений (seenNotifyWhy).
- **Иконка приложения**: концепт «Колесо» утверждён направлением; раунды правок
  в `docs/icons/variants{,2,3}` (без красного, конверт=прямоугольник ✉;
  превью ICON_VARIANTS3.png в корне) — ждёт выбора f1–f4 → экспорт мипмап.
- Все 11 движковых тестов зелёные, svelte-check 0/0. Смоук puppeteer: скраб
  двигает шапку, тумблер выделения, первый запуск (glow/cardhelp/ndot).

## Сделано 2026-07-12 (смена иконки приложения: «треугольник» f1)
- Владелица сменила выбор лаунчера с «ромба» f2 на **«треугольник» f1**
  (`docs/icons/variants3/wheel-f1.svg`): Солнце в вершине, золотой треугольник
  0°/120°/240°, циановая хорда 240°→60°, кольцо с 12 рисками, космос-фон.
- Перегенерирован ВЕСЬ комплект тем же пайплайном (headless Chrome, scratchpad
  `gen_launcher_f1.mjs`): ic_launcher + ic_launcher_round (mdpi..xxxhdpi),
  adaptive-слои foreground/bg/monochrome, `public/icon.svg` (favicon/PWA).
  anydpi-v26 XML не менялись (имена слоёв те же).
- На телефон приедет ТОЛЬКО с новым APK (натив, не OTA); сайт/PWA — со
  следующим деплоем Pages.
