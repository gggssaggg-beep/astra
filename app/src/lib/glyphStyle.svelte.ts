/**
 * Текущий стиль окраски глифов (серебро/золото/стихии/радуга) — модульный
 * $state-стор. App резолвит settings.signStyle ('auto' → по теме, экономия →
 * 'gold') и кладёт сюда; Glyph.svelte читает как дефолт, если стиль не передан
 * пропом. Так инлайновые глифы в 20+ шторках красятся единообразно БЕЗ
 * прокидывания пропа через всё дерево.
 */
import type { SignStyle } from './models.ts';

export const glyphStyle = $state<{ v: SignStyle }>({ v: 'silver' });
